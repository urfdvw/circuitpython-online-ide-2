{
    "name": "backup",
    "title": "Backup",
    "description": "Backup folder settings.",
    "type": "object",
    "properties": {
        "enable_schedule": {
            "title": "Periodic automatic backup",
            "description": "Check this item to enable periodic backup.",
            "type": "boolean",
            "default": false
        },
        "clean": {
            "title": "Clean folder before each backup",
            "description": "When selected, all contents within the backup folder will be deleted prior to backup. If not selected, the backup process will only overwrite files with matching names.",
            "type": "boolean",
            "default": false
        }
    }
}
