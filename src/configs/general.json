{
    "name": "general",
    "title": "General",
    "type": "object",
    "properties": {
        "theme": {
            "title": "Global theme",
            "description": "Choose 'system' to follow OS theme settings",
            "type": "string",
            "enum": ["system", "dark", "light"],
            "default": "system"
        },
        "show_time": {
            "title": "Show time in menu bar",
            "type": "boolean",
            "default": true
        },
        "show_board_id": {
            "title": "Use board id as title",
            "type": "boolean",
            "default": false
        },
        "whats_new_2_5_0": {
            "title": "Show version change info",
            "type": "boolean",
            "default": true
        },
        "enable_agent_bridge": {
            "title": "Enable AI Agent Bridge",
            "type": "boolean",
            "default": false
        }
    }
}
